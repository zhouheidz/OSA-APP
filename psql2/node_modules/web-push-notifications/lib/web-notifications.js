'use strict';

var _Global = require('./classes/Global'),
    _Global2 = _interopRequireDefault(_Global),
    _functions = require('./utils/functions');

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var global = (0, _functions.getGlobal)(),
    Pushwoosh = global.Pushwoosh,
    predefinedCommands = void 0;

if (Pushwoosh) {
  predefinedCommands = Pushwoosh;
}

Pushwoosh = new _Global2.default();
if (Array.isArray(predefinedCommands)) {
  predefinedCommands.forEach(function (c) {
    return Pushwoosh.push(c);
  });
}

global.Pushwoosh = Pushwoosh;